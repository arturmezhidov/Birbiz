import { IMessagesState, MESSAGES_INITIAL_STATE } from './messages.state';

export function messagesReducer(state: IMessagesState = MESSAGES_INITIAL_STATE, action: any): IMessagesState {
    return state;
}